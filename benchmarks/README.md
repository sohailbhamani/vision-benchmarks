# Benchmarks

This directory will contain benchmark results for various vision AI models.

## Planned Benchmarks

- YOLO variants (speed and accuracy comparisons)
- OCR models (curved/rotated text accuracy)
- Embedding models (similarity search performance)
- Segmentation models (SAM, YOLO-Seg)

Results will be documented in markdown files with tables and charts.
